# 并行开发说明

## 1. 目标

本项目采用三个会话并行开发，在 30 分钟内完成可运行、可测试、可通过 Docker Compose 交付的 Banner 指纹识别系统。

协作原则：

- 一个主会话负责架构、公共接口、核心识别和最终集成。
- 两个工作会话分别负责 API/Client 和容器化交付。
- 每个会话只修改自己负责的文件，避免并行覆盖。
- 优先完成必须功能，不引入数据库、消息队列、管理后台、热更新等非必要能力。

## 2. 开发前置条件

主会话应先完成以下工作，再启动其他会话：

1. 初始化 Git 仓库并创建基线提交。
2. 创建项目目录和 `go.mod`。
3. 固定公共数据结构和识别引擎接口。
4. 将本说明提交到基线。
5. 为并行会话创建独立 Worktree 或独立分支。

禁止多个会话直接在同一个工作目录中同时修改文件。

## 3. 共享接口约定

公共模型建议位于 `internal/model/model.go`：

```go
package model

type ScanInput struct {
	IP     string `json:"ip"`
	Port   int    `json:"port"`
	Banner string `json:"banner"`
}

type FingerprintResult struct {
	IP         string  `json:"ip"`
	Port       int     `json:"port"`
	Protocol   string  `json:"protocol"`
	Product    string  `json:"product"`
	Version    string  `json:"version"`
	OSHint     string  `json:"os_hint"`
	Confidence float64 `json:"confidence"`
}
```

识别引擎接口建议位于 `internal/fingerprint/engine.go`：

```go
type Engine interface {
	Identify(model.ScanInput) model.FingerprintResult
}
```

所有会话必须遵守以下行为约定：

- 输出顺序与输入顺序一致。
- 无法识别时返回 `protocol: "unknown"`。
- unknown 结果的 `product`、`version`、`os_hint` 为空字符串。
- `confidence` 范围为 `0` 到 `1`。
- 单条 unknown 不得导致整批请求失败。
- 不得只根据端口认定协议；端口最多作为弱辅助信息。

如果确实需要修改公共接口，应先停止依赖该接口的工作，并由主会话统一修改和通知，不能由工作会话自行变更。

## 4. 会话职责

### 4.1 主会话：核心与集成

负责内容：

- 创建项目骨架和公共模型。
- 实现规则加载、校验和正则预编译。
- 实现指纹识别引擎。
- 覆盖 SSH、HTTP、MySQL、Redis、FTP。
- 维护 `rules/fingerprints.json`。
- 编写识别引擎表驱动测试。
- 合并其他会话的提交。
- 解决集成冲突并执行最终验收。
- 完成最终 Git 提交和 GitHub 上传。

允许修改：

```text
go.mod
go.sum
internal/model/
internal/fingerprint/
rules/
testdata/
并行开发说明.md
```

在最终集成阶段，主会话可以修改仓库内任意文件。

核心识别顺序：

1. MySQL 等二进制协议结构。
2. 外部 Banner 规则。
3. HTTP 等通用协议特征。
4. 弱辅助特征。
5. unknown。

### 4.2 会话 A：Server 与 Client

负责内容：

- 实现 `POST /fingerprint`。
- 实现 `GET /health`。
- 实现请求体大小和批量数量限制。
- 保证 panic 不会导致服务退出。
- 配置 HTTP 读写、空闲超时和优雅退出。
- 实现独立 Client：读取 JSON 文件、调用 Server、输出格式化 JSON。
- 编写 Handler 和 Client 相关测试。

允许修改：

```text
cmd/server/
cmd/client/
internal/api/
internal/client/
```

不得修改：

- `internal/model/` 中的公共字段。
- `internal/fingerprint/` 中的识别逻辑。
- 指纹规则文件。
- Docker 和 Compose 文件。

接口行为：

- `GET /health`：引擎和规则可用时返回 HTTP 200。
- `POST /fingerprint`：有效数组返回 HTTP 200。
- 非法 JSON、非数组、超出限制返回 HTTP 400。
- 方法不匹配返回 HTTP 405。
- 合法但无法识别的数据仍返回 HTTP 200 和 unknown 结果。

完成标准：

```powershell
go test ./cmd/... ./internal/api/... ./internal/client/...
```

完成后提交一次独立 Git commit，并将提交哈希交给主会话。

### 4.3 会话 B：Docker、Compose 与说明

负责内容：

- 编写多阶段 `Dockerfile`。
- 编写 `compose.yaml`。
- 配置真实健康检查。
- 收紧容器权限。
- 编写 `.dockerignore`。
- 编写项目 `README.md`。
- 提供构建、启动、Client 调用和规则扩展示例。

允许修改：

```text
Dockerfile
compose.yaml
.dockerignore
README.md
```

不得修改 Go 业务代码和规则文件。若健康检查需要 Server 提供特定命令或参数，只记录需求并通知主会话处理。

容器要求：

- Go Builder 与最小 Runtime 的多阶段构建。
- `CGO_ENABLED=0`，运行镜像不包含编译工具。
- 使用非 root 用户。
- `read_only: true`。
- `cap_drop: [ALL]`。
- `no-new-privileges:true`。
- 只暴露 Server 必需端口。
- Client 通过 Compose 服务名访问 Server。
- Client 等待 Server 健康后再运行。
- 健康检查必须访问 `/health`，不能只检查进程是否存在。

完成标准：

```powershell
docker compose config
docker compose build
docker compose up -d server
docker compose ps
```

完成后提交一次独立 Git commit，并将提交哈希交给主会话。

## 5. 推荐时间安排

| 时间 | 主会话 | 会话 A | 会话 B |
|---|---|---|---|
| 0–5 分钟 | 骨架、公共接口、基线提交 | 等待接口确认 | 等待接口确认 |
| 5–18 分钟 | 识别引擎、规则、单测 | Server、Client、测试 | Docker、Compose、README |
| 18–23 分钟 | 合并两个工作会话 | 修复反馈 | 修复反馈 |
| 23–27 分钟 | 全量测试和端到端验证 | 协助排错 | 协助排错 |
| 27–30 分钟 | 最终提交、推送、确认仓库可访问 | 停止新增功能 | 停止新增功能 |

到第 23 分钟后冻结功能，只处理构建、测试、容器启动和文档中的阻塞问题。

## 6. 合并顺序

主会话按以下顺序集成：

1. 确保核心引擎测试通过。
2. 合并 Server/Client 会话提交。
3. 运行全部 Go 测试。
4. 合并 Docker/Compose/README 会话提交。
5. 构建镜像并启动 Server。
6. 使用 Client 运行示例数据。
7. 检查 unknown、乱码和非标准端口输入。
8. 检查容器健康状态和非 root 身份。
9. 检查 Git 状态，提交最终版本并推送。

工作会话不得自行合并其他分支或推送最终主分支。

## 7. 最终验收清单

主会话在上传前逐项确认：

- [ ] `go test ./...` 通过。
- [ ] `docker compose config` 通过。
- [ ] `docker compose build` 通过。
- [ ] `docker compose up -d server` 后健康状态正常。
- [ ] Client 能通过 Compose 服务名访问 Server。
- [ ] 示例中的 SSH、HTTP、MySQL、Redis、FTP 能正确识别。
- [ ] 非标准端口上的明确 Banner 仍能识别。
- [ ] 未知和畸形 Banner 不会导致崩溃。
- [ ] 输出字段完整，置信度在合法范围内。
- [ ] 指纹规则位于外部配置文件。
- [ ] Server 容器以非 root 用户运行。
- [ ] README 中的命令可以直接复制执行。
- [ ] GitHub 仓库可被评估者访问。

## 8. 各会话建议提示词

### 会话 A

```text
你负责 Banner 指纹系统的 Server 与 Client。先阅读 要求.md 和 并行开发说明.md，严格遵守其中的公共接口和文件边界。只修改 cmd/server、cmd/client、internal/api、internal/client。完成实现和测试后提交一个独立 commit，报告测试结果和提交哈希，不修改核心识别、规则、Docker 或 README。
```

### 会话 B

```text
你负责 Banner 指纹系统的 Docker、Compose 和 README。先阅读 要求.md 和 并行开发说明.md，严格遵守文件边界。只修改 Dockerfile、compose.yaml、.dockerignore、README.md。实现多阶段构建、非 root、真实健康检查、权限收紧以及 Server/Client 使用说明。完成验证后提交一个独立 commit，报告验证结果和提交哈希，不修改 Go 业务代码或规则。
```
